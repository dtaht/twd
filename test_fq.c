/* Test fq by setting up one high rate and one low rate stream

   setup_interval(hook,100ms); // 
   flood(duration);

hook() {
   get_time()
   if(duration < end_of_test) send_packet();
 }
 
   see if rtts stay somewhat flat for the slow stream
*/ 



