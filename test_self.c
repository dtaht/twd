/* Do some self tests and timings
   Measure:

   syscall overhead
   get_time resolution (already in test_clock)
   preservation of ecn and DSCP bits through localhost (important!
	many OSes silently pee on these bits)
   bandwidth via localhost
   */

